- Added Move Kotlin File refactoring: move the active `.kt` file from **Refactor → Move Kotlin File...** to a selected source root and package; its matching package directive and supported Kotlin code references are updated, and **Undo Last Refactoring** restores the original location and contents.
- Improved Kotlin Inline Variable: local `var` declarations with one initializer and no subsequent writes can now be inlined from either a declaration or selected usage; assignments and increment/decrement writes are rejected before changing the source with the conflicting expression identified.
- Improved Kotlin Introduce Type Alias: selecting a complete generic type now creates a concrete alias, while selecting only its type constructor creates a parameterized alias that preserves nested and qualified type structures and each same-file usage's own type arguments; star projections and local type parameters that cannot form a valid top-level alias are rejected.
- Improved Kotlin hierarchy-aware refactorings: Pull Members Up, Push Members Down, and compatible IDEA refactoring paths now discover Kotlin override chains and direct supertypes across all source files in the active Maven or Gradle build; Java/library and full IDEA-index conflict scenarios remain unavailable.
- Improved Kotlin Extract Interface and Extract Superclass: source and target Kotlin files now roll back together if an update fails, generated supertypes remain correctly separated from existing target declarations, and Undo Last Refactoring precisely restores an existing target or removes a generated one.
- Improved Kotlin Change Signature, Introduce Parameter, and Introduce Functional Parameter: when updating any touched Kotlin file fails, all declaration and caller changes now roll back together while preserving formatting outside the changed hunks.

# 0.11.36 (2026-08-01)

- Improved Kotlin Copy Declaration: choose a target source root, package (including a new package), and filename; copying into a new target removes it on undo, while copying into an existing target restores that file's original contents; failed copies roll back their target changes.
- Fixed Undo Last Refactoring for Kotlin Move Declaration across source and target files.
- Added a Kotlin refactoring coverage matrix that tracks IntelliJ IDEA parity, tests, known limitations, and the planned work needed to complete each refactoring family.
- Added **Push Members Down** for Kotlin: place the caret in a superclass or interface, invoke **Refactor → Push Members Down...** or press **Ctrl+Alt+O**, select members and optional per-member **Make Abstract** declarations, and push the selected members into every direct Kotlin subclass in the related build; the K2-backed operation formats every changed document and **Refactor → Undo Last Refactoring** restores all edited source and child files.
- Added **Pull Members Up** for Kotlin: place the caret in a child class, invoke **Refactor → Pull Members Up...** or press **Ctrl+Alt+U**, select a Kotlin superclass or interface (including one in another file), choose members and optional abstract declarations, and apply the K2-backed move; direct target-member conflicts are checked automatically before the move, while **Preview Conflicts** remains available as an optional early check; **Refactor → Undo Last Refactoring** restores both edited files.
- Added **Extract Interface** and **Extract Superclass** refactorings for Kotlin: place the caret in a class, invoke **Refactor → Extract Interface...** or **Refactor → Extract Superclass...**, choose the members, target name, package, and source root, and the selected declarations are moved into a newly created Kotlin interface or superclass while the source class inherits from it; generated code follows the project's Kotlin formatting settings and **Refactor → Undo Last Refactoring** restores both the source and generated file.
- Added **Introduce Functional Parameter** refactoring (Ctrl+Alt+Shift+P / **Refactor → Introduce Functional Param**) for Kotlin: select an expression inside a function, secondary constructor, or a class's primary-constructor scope, invoke the action, and edit the parameter name, whether to replace all matching occurrences in the body, and whether the parameter gets a declared default value — the expression is wrapped in a functional type (e.g. `(Int) -> Int` when the expression reads a captured variable, `() -> Int` when it doesn't), the body occurrence(s) are replaced with a call to the new parameter, and every existing call site receives a lambda literal built from the original expression; powered by the same IntelliJ IDEA K2 Extract Function engine that backs Extract Function, driven with the `FAKE_LAMBDALIKE_FUNCTION` extraction target the same way real IDEA's Introduce Lambda Parameter does; undo rolls every touched file back atomically.
- Added **Introduce Parameter** refactoring (Ctrl+Alt+P / **Refactor → Introduce Parameter...**) for Kotlin: select an expression inside a function, secondary constructor, or a class's primary-constructor scope (property initializer/class body), invoke the action, and edit the parameter name, type (editable combo pre-filled with the inferred type and its supertypes), whether to replace all matching occurrences in the body, and whether the parameter gets a declared default value — the expression is replaced by a reference to the new parameter and every existing call site is updated to pass the original expression explicitly; powered by the same IntelliJ IDEA K2 Change Signature engine that backs Change Signature (Ctrl+F6), driven through the real ported `IntroduceParameterDescriptor`; undo rolls every touched file back atomically.
- Fixed semantic highlighting and the Navigator not working for `.kt` files in Gradle projects: the plugin recognized Gradle projects only by the old third-party "NetBeans Gradle Support" plugin's project class, which current Apache NetBeans's built-in Gradle module (bundled since NB 12+) doesn't use, so its classpath/dependencies were never picked up; also fixed the built-in module's classpath lookup only checking generic source groups, missing the Java/Kotlin-typed ones it actually registers, which still left the classpath empty after the project-recognition fix; also fixed highlighting/Navigator still not working the *first* time a Gradle project was opened, because the built-in Gradle module resolves its classpath asynchronously and the plugin pre-warmed its session before that finished, permanently caching an empty-dependency session — the session is now refreshed once the Gradle module reports its project info is ready; both the built-in module and the old third-party plugin are now supported.
- Fixed the Navigator getting permanently stuck on "Please Wait..." for a file when other `.kt` files were parsed in the background around the same time (e.g. during startup pre-analysis of many files) — the parser was tracking the currently parsed file/project in shared state instead of per-file, so a concurrently parsed file could silently steal another file's result.
- Fixed hover documentation and Ctrl+Click "Go to Declaration" not working for symbols declared in another Gradle subproject of the same multi-module build (e.g. a class from a shared `observability-core`-style module used by a `kafka`-style module) — the plugin only added `.jar` files to the K2 session's classpath, but Gradle's IDE-mode compile classpath represents subproject dependencies as a directory of compiled classes rather than a jar, so those symbols were silently unresolvable; also fixed the same symbols still being unresolvable for a **Kotlin-only** subproject dependency, because NetBeans's Gradle support only reports that subproject's (empty) Java output directory, never its actual Kotlin output directory; also fixed a sibling subproject's classpath directory being incorrectly dropped again by an overly broad "is this my own compiled output" check; also fixed the Kotlin output directory still not being picked up for a Kotlin-only subproject whose Java output directory doesn't exist on disk at all (a genuinely Kotlin-only module never runs a Java compile step); also fixed Ctrl+Click "Go to Declaration" specifically (hover already worked) still not landing anywhere for those cross-subproject symbols — they were resolved via the subproject's *compiled* output, which has no attached source location to navigate to, so a subproject's compiled-output directory is now promoted to a real source module (using its actual `.kt` sources) whenever that source is available on disk.
- Added **Change Signature** refactoring (Ctrl+F6 / **Refactor → Change Signature...**) for Kotlin functions and constructors: place the caret on a declaration (including a class name, for its primary constructor) or on any call site, invoke the action, and edit the name, return type, and parameter list (add, remove, rename, retype, and reorder parameters via toolbar buttons or drag-and-drop) in the dialog — every call site (including call sites resolving through an overriding declaration and by-convention operator calls like `box["key"]`), in-body reference to a renamed parameter, overriding declaration (updated project-wide), `this(...)`/`super(...)` constructor-delegation call, `val (a, b) = point` destructuring (reordered to keep each variable bound to the same value), enum entry without an explicit super call, and direct caller (which grows the same new parameter and forwards it down to its own call to the changed function) is updated across the project; a change that would create a duplicate parameter name is rejected instead of applied; only the actually-changed regions of each file are reformatted, leaving unrelated surrounding code untouched; **Refactor → Undo Last Refactoring** reverts every touched file; powered by the IntelliJ IDEA K2 change-signature engine.
- Fixed pressing Enter between `{` and `}` (e.g. an empty method body inside a class) placing the closing brace at column 0 instead of aligning it with the enclosing line, and leaving the indentation impossible to fix by typing — caused by the paste-indentation logic mistaking the editor's own auto-indent edit for a pasted block and rewriting it; also fixed the newly-indented line using a hardcoded 4-space step instead of the project's configured indent size.
- Added **Move Declaration** refactoring (**Refactor → Move Declaration...**) for Kotlin: place the caret anywhere inside a top-level named declaration (function, class, object, or top-level property), **or on any usage/call site of it anywhere in the project**, invoke the action, enter a target package and target file name in the dialog, and the declaration is moved to that file (created alongside the declaration's own file), with external references across the project retargeted to the new location; the preview pane lists every usage that will be updated; conflicts (visibility, name clashes, sealed-class hierarchy breaks, and others) are detected via the IntelliJ IDEA K2 move engine and shown before the move is applied.
- Fixed a standalone-environment-wide issue where any bundled IDEA diagnostic log call (`Logger.error()`) could crash the plugin instead of just being logged, by installing a real logger implementation; surfaced while adding Move Declaration's cross-file reference updates, but affects any feature that touches the same code paths.
- Added **Introduce Property** refactoring (Ctrl+Alt+F / **Refactor → Introduce Property**) for Kotlin: select an expression inside a class method body, invoke the action, enter a property name in the dialog, choose `val` or `var`, optionally pick a destination scope when multiple class bodies are nested — the expression is extracted into a `val/var NAME: Type = expr` property inserted before the enclosing member, and the selection is replaced by the property name; powered by the IntelliJ IDEA K2 `ExtractionDataAnalyzer` with `extractAsProperty = true`; undo rolls the file back atomically.
- Added **Introduce Type Alias** refactoring (Ctrl+Alt+Shift+A / **Refactor → Introduce Type Alias**) for Kotlin: place the caret on a type reference (simple or generic, e.g. `String`, `List<String>`), invoke the action, enter an alias name in the dialog, choose visibility (`public`/`internal`/`private`), and optionally replace all identical type references in the file; a `typealias NAME = TYPE` declaration is inserted before the enclosing top-level declaration; undo rolls the file back atomically.
- Added **Copy Declaration** refactoring (**Refactor → Copy Declaration**) for Kotlin: place the caret anywhere inside a top-level named declaration (function, class, object, or top-level property), invoke the action, confirm or edit the suggested target file name, and the declaration is copied to a new `.kt` file in the same directory with the correct package directive; ported from the IntelliJ IDEA K2 `CopyKotlinDeclarationsHandler` — when the declaration is the only one in its file the whole file is copied so all imports carry over, otherwise the IDEA `K2MoveRenameUsageInfo` engine copies the declaration and rebinds its internal references; undo deletes the created file.
- Added **Introduce Import Alias** refactoring (**Refactor → Introduce Import Alias**) for Kotlin: place the caret on an `import pkg.Class` directive **or on any reference** to that class anywhere in the file, invoke the action, enter the desired alias name in the dialog (pre-filled with the current short name), and the import is changed to `import pkg.Class as Alias` while every occurrence of the short name in the file is renamed to the alias; ported from the IntelliJ IDEA K2 `KotlinIntroduceImportAliasHandler` — the trigger-from-any-reference behaviour (K2 resolves the FQN and locates the corresponding import directive automatically) matches IDEA; undo rolls the file back atomically.
- Added **Introduce Constant** refactoring (Ctrl+Alt+C / **Refactor → Introduce Constant**) for Kotlin: place the caret on (or select) a compile-time constant expression (numeric or string literal, or a constant arithmetic expression), invoke the action, and the expression is extracted into a `const val NAME: Type = expr` declaration inserted at the chosen destination — **Companion object** (as the first member of the companion object in the enclosing class, creating the companion object if it does not yet exist) or **Top-level of file** (as a top-level declaration just before the enclosing class); the dialog offers the destination as a combo box when both options are available, a name field pre-filled with a SCREAMING_SNAKE_CASE suggestion, and a **Replace all occurrences** checkbox (default on) that replaces all semantically equivalent occurrences in the file; undo rolls the file back atomically.
- Added **Extract Function** refactoring (Ctrl+Alt+M / **Refactor → Extract Function**) for Kotlin: select one or more statements or expressions, invoke the action, enter a function name in the dialog, optionally choose a destination scope (local function inside the enclosing block, member of the enclosing class, or top-level in the file) via the **Destination** combo box that appears when multiple scopes are available, and the selection is replaced with a call expression while the new function definition is inserted in the chosen scope; parameters and return type are inferred by the IntelliJ IDEA K2 extraction engine; undo rolls the file back atomically.
- Added **Introduce Variable** refactoring (Ctrl+Alt+V / **Refactor → Introduce Variable**) for Kotlin expressions powered by the K2 Analysis API: extracts the expression under the caret (or selection) into a new declaration inserted just above the enclosing statement, replaces all semantically equivalent occurrences in the same scope using `K2SemanticMatcher`, suggests a name from `KotlinNameSuggester`, and lets the user edit the name in an inline panel before applying; the dialog now offers three IDEA-matching options — **Replace all occurrences** (checkbox, default on), **val / var** (radio buttons, default val), and **Specify type explicitly** (checkbox, default off, adds the inferred type annotation); preview panel shows every occurrence; undo rolls the file back atomically. The **Refactor** menu is reordered to match IntelliJ IDEA's Kotlin menu (Rename → Move → Safely Delete → Extract Function → Introduce Variable → Inline → Undo/Redo).
- Added **Inline Function** refactoring (Ctrl+Alt+N / **Refactor → Inline...**) for Kotlin named functions powered by the IntelliJ IDEA K2 `codeInliner/` engine: every call site is replaced with the inlined function body using IDEA's `CallableUsageReplacementStrategy` + `CodeInliner` (which handles argument substitution, type parameter expansion, qualified-call rewriting, and import insertions) and the function declaration is removed; preview panel lists every occurrence; undo rolls all affected files back atomically; the refactoring can be invoked with the caret on the function declaration **or** on any call site. The existing Inline Variable action is unified: Ctrl+Alt+N now dispatches to function or variable inline based on what is under the caret.
- Fixed all quick-fix actions (Remove Argument, Add Return, Add val/var, Add @JvmInline, Add inline modifier, Add suspend, Add When branches, and others) inserting spurious blank lines throughout the edited file when the file contained function bodies with blank-line separators between statements.
- Added **Inline Variable** refactoring (Ctrl+Alt+N / **Refactor → Inline Variable**) for Kotlin `val` properties powered by the IntelliJ IDEA K2 inline engine: every read reference is replaced with the property's initializer using IDEA's `CodeInliner` (which keeps the substituted expression syntactically and semantically correct — parenthesisation, qualified-call rewriting, import insertions) and the declaration is removed; preview panel lists every occurrence; undo rolls all affected files back to their pre-refactor state; the refactoring can be invoked with the caret on the declaration **or** on any usage of the variable.
- Added Rename refactoring (Alt+Shift+R) powered by the K2 Analysis API: renames a Kotlin symbol (class, function, property, parameter, local variable) and all its references across the project in one step, including the override cascade (renaming an overriding function also renames its super- and sibling overrides); when the renamed symbol is a class whose name matches the containing file's base name, the file is renamed too. The rename dialog shows the current name pre-selected with focus, offers a **Search in comments** checkbox (state persisted across IDE sessions) that also updates plain-text occurrences inside line/block/KDoc comments, and disables Preview/Refactor when the new name is empty or unchanged. Edit → Undo (Ctrl+Z) reverts the rename per file; Refactor → **Undo Last Refactoring** / **Redo Last Refactoring** roll back or re-apply the entire refactoring across all files (including the file rename) in one click.
- Added **Inspect Hierarchy** (Alt+Shift+F12): opens a dockable "Type Hierarchy" window showing an interactively expandable tree of subtypes or supertypes for the Kotlin class or interface at the caret; supports lazy expansion, Back/Focus navigation, and switching between Subtypes/Supertypes modes; double-clicking a node navigates to its source in the editor.
- Added Navigate menu parity with Java for Kotlin files: **Go to Declaration** (Ctrl+B) and **Go to Source** (Ctrl+Shift+B) jump from a reference to its declaration; **Go to Test/Tested class** (Ctrl+Alt+T) opens the matching `Foo` ↔ `FooTest` counterpart; **Go to Super Implementation** (Ctrl+Shift+P) jumps to the overridden declaration; **Go to Implementation** (Ctrl+Alt+B) lists overriding implementations across the project; **Inspect Members** (Ctrl+Shift+F12) focuses the Navigator window. All items are also available in the editor right-click **Navigate** submenu.
- Added Find Usages (Alt+F7): pressing Alt+F7 on any Kotlin symbol (function, class, property, method) now opens the Find Usages panel with all references to that symbol across the project; clicking a result navigates directly to the usage site.
- Fixed Rename refactoring (Alt+Shift+R) which was silently unregistered due to annotation processing being disabled in the build (`-proc:none`); rename now works correctly for Kotlin symbols.
- Added **Find Usages** to the editor right-click context menu and to the file-node right-click menu in the Projects/Files panels (mirrors the Java context menu).
- Fixed vertical indent-guide lines in `.kt` files ignoring the per-project Kotlin indent (`.idea/codeStyles/Project.xml`) and using the global indent instead: when a project is opened, the editor's indent settings are now refreshed immediately so the guides reflect the per-project value on the first paint.
- Added New File wizard templates for Data class, Annotation class, Sealed class, Sealed interface, and Abstract class; all ten Kotlin templates (including the existing Class, Interface, Enum, Object, and plain File) are now visible in **New → Other → Kotlin**; fully-qualified class names (e.g. `com.example.MyClass`) are accepted in the name field and automatically split into package and file name.
- Fixed false-positive "Unused import" hints for imports whose symbols could not be resolved by the K2 session (e.g. external projects with partial classpath): the detector now uses a conservative two-pass algorithm based on short-name presence in the file body, with optional shadowing check.
- Added full IDEA-style Kotlin formatter settings UI: **Tools → Options → Kotlin** now contains a scheme selector (Kotlin Coding Conventions / Kotlin Obsolete Coding Conventions / IDE Defaults) with IDEA-style gear menu (Duplicate, Rename, Delete, Restore Defaults, Export/Import in IntelliJ IDEA XML format), and six settings tabs — Tabs & Indent, Spaces, Wrapping & Braces, Blank Lines, Imports, Other — with a live-preview pane that reformats a sample on every change; the Spaces and Wrapping & Braces tabs are auto-generated from IDEA's Kotlin code-style provider and include all IDEA-equivalent options; per-project overrides are available under **Project Properties → Kotlin Formatter** (Maven, J2SE/Ant, and Gradle) and are stored in `.idea/codeStyles/Project.xml` in IntelliJ IDEA format.

# 0.10.12 (2026-05-30)

- Fixed semantic highlighting being completely absent for Kotlin files where K2 symbol resolution crashes on a single element (e.g. due to FIR phase ordering in projects targeting JVM 1.8): before-resolve highlights (class/function/property/parameter names, annotation entries) are now always applied, and K2-resolve highlights accumulate up to the failing element instead of being discarded entirely.
- Added 17 K2 quick-fixes matching IDEA: modifier fixes (add `@JvmInline`, add `val`/`var` to constructor parameter, add `suspend`, add `noinline`/`crossinline`, convert object with constructor to class, add return expression), expression fixes (remove extra argument, convert to function invocation, move nested type alias to top level, convert char literal to string), supertype/constructor fixes (remove supertype, remove constructor invocation), property fixes (add getter/setter accessors, add explicit type annotation to parameter), and `when` fix (add remaining branches).
- Fixed "Unresolved reference" errors in Maven projects where `kotlin-maven-plugin` lists source directories as plain relative paths (e.g. `src/main/kotlin`) in `<sourceDirs>` without the `${project.basedir}` prefix: those paths are now correctly resolved against the project base directory.
- Added 5 modifier/visibility intentions: add `open` modifier to a final function or property in an open/abstract/sealed/enum class; and four "Make public/private/protected/internal" intentions that replace or remove the explicit visibility keyword respecting implicit visibility defaults and override constraints.
- Added 6 expression/operator intentions: apply De Morgan's law on `&&`/`||`, convert operator to explicit function call (`a + b` → `a.plus(b)`), flip binary expression operands (`a > b` → `b < a`), insert/remove explicit type arguments, and specify explicit lambda parameter signature.
- Added 30 intentions: 7 when/if refactoring intentions (add remaining `when` branches, convert `if`-else chain ↔ `when`, flatten nested `when`, merge/split `if`s, invert `if` condition), 6 string transformation intentions (convert concatenation ↔ string template, convert to raw `"""` literal, convert concatenation or template to `buildString { }`), 8 lambda/function-reference intentions (convert lambda ↔ callable reference `::`, lambda → anonymous function, lambda body single-line ↔ multi-line, `.forEach {}` ↔ `for` loop), 5 property/declaration intentions (convert property getter ↔ initializer, add getter/setter stubs, split property declaration into declaration + assignment, move body property into primary constructor parameter), and 4 braces intentions (add/remove braces on a single branch, add/remove braces on all branches of an if-chain or when expression).
- Added parameter info popup (Ctrl+P): pressing Ctrl+P inside a Kotlin function call now shows the function's parameter list with the current parameter highlighted.
- Improved time to first semantic highlighting for a Kotlin file: removed a global scan-in-progress check that blocked the parser while any project was being indexed, added background pre-warming of the K2 analysis session when a `.kt` editor opens so session initialisation runs in parallel with NetBeans project scanning, and applied semantic highlighting in the background for `.kt` files already open when NetBeans starts (restored from the previous session) so colours appear without requiring a click on the tab.

# 0.9.13 (2026-05-27)

- Added hover tooltip: pausing the mouse over a Kotlin symbol now shows a documentation popup with a syntax-highlighted signature, container information, and KDoc sections; each completion candidate also shows its own documentation rather than always showing the first candidate's docs.
- Improved code completion: dot-receiver (`expr.`) now shows only members and applicable extensions of the receiver type; each item displays its signature (`(param: Type): ReturnType` or `: Type`); Kotlin-accurate icons distinguish val, var, method, extension function, interface, enum, object, type alias, and parameter; items are deduplicated and sorted with locals and type members first.
- Added automatic indentation of code pasted into the `.kt` editor: both single-line and multi-line pastes are shifted to the correct indentation level at the insertion point; the internal relative indentation of the pasted block is preserved (no reformatting).
- Added Format option to the `.kt` editor right-click context menu (after Refactor); when text is selected, only the selected range is reformatted (Alt+Shift+F already worked; this adds the menu entry and range support).
- Fixed `SEVERE: ClassNotFoundException: org.codehaus.plexus.util.PropertyUtils` logged on J2SE project open: replaced the inaccessible Maven Embedder utility with a standard `Properties.load()` call.
- Added History tab to `.kt` file editor: file-change history is now accessible directly from the editor alongside the Source tab.
- Removed empty "Visual" tab from `.kt` file editor; only the "Source" tab (and "History" when the versioning module is installed) now appears.

# 0.9.5 (2026-05-25)

- Added IntelliJ IDEA-style Kotlin highlighting settings: Tools > Options > Fonts & Colors now lists the same named Kotlin categories as IDEA (keywords, numbers, string escapes, classes, properties, function calls, smart casts, labels, etc.) with IDEA's default colors for light and dark themes, plus an IDEA-matching preview sample.
- Fixed K2 semantic highlighting drifting out of place while typing unsaved changes: colors below the edit point now stay correctly positioned as you type.

# 0.8.15 (2026-05-22)

# 0.8.6 (2026-05-20)

- Fixed JDK standard library types not visible in the K2 analysis session, causing false type errors and broken semantic highlighting for code that uses JDK types.
- Added support for Kotlin 2.3.x source files.

# 0.7.13 (2026-05-19)

- Switched all language features (diagnostics, completion, semantic highlighting, hints/quick-fixes, navigation) to the K2 Analysis API, replacing the previous K1 engine.
- Added support for Kotlin 2.0.x source files.
- Added code folding for Kotlin files: collapse/expand (+/-) controls in the editor gutter for the import list, comments and code blocks, with the fold types listed in Tools > Options > Editor > Folding.
- Added K2 Navigator panel support: classes, functions and properties in `.kt` files are now listed in the Navigator panel using the Analysis API.

# 0.6.8 (2026-05-12)

- Upgraded maximum supported Kotlin version from 1.3 to 1.9.

# 0.5.22 (2026-05-08)

- Compiled kotlin-converter from submodule sources (A4.6)

# 0.5.4 (2026-05-02)

- Changed MIME type from `text/x-kt` to standard `text/x-kotlin`, overriding NetBeans' built-in basic Kotlin support with full CSL-based features

# 0.4.5 (2026-05-02)

- Added GitHub Actions CI/CD workflow (A2)
